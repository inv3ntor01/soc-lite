apiVersion: v1
kind: Service
metadata:
  name: crowdsec-service
  namespace: security
spec:
  selector:
    app: crowdsec
  ports:
    - name: api
      port: 8080
      targetPort: 8080
      protocol: TCP
